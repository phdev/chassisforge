/**
 * Mobility calculations — not applicable to the Comni Box (stationary device).
 * Kept as a stub for compatibility with any remaining imports.
 */

export function calculateMaxSpeed(): number { return 0; }
export function calculateTurningRadius(): number { return 0; }
export function calculateGradeability(): number { return 0; }
export function calculateStepClearance(): number { return 0; }
